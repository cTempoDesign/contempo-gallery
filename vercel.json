{
  "buildCommand": "npm run test:coverage && cd example && npm install && npm run build",
  "outputDirectory": "example/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}